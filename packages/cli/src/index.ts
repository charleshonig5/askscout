console.log("🐕 Scout is sniffing through your commits...");

// TODO: implement CLI argument parsing and main flow
// - Parse args (--resume, --standup, --today, --week, --json, --dry-run, --setup)
// - Read API key from ~/.askscout/config.json (or prompt for setup)
// - Call core: getCommits → getDiffs → summarize → format
// - Print output to terminal
